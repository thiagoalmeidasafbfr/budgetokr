// SQLite removed — project now uses Supabase (PostgreSQL)
// This stub exists to satisfy any lingering imports during migration.
export function getDb(): never {
  throw new Error('getDb() is not available — use Supabase client via lib/supabase.ts')
}
